struct levelDB {
  char adresse[20];
  unsigned long lgrad, bgrad;
  int level, year, month, date, hour, minute;
  
};

struct idDB {
  unsigned long ID;
};


void setup() {
  // put your setup code here, to run once:
 struct idDB number1, number2, number3;
 struct levelDB;
}

void loop() {
  // put your main code here, to run repeatedly:

}
